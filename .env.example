# Core infrastructure
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0
REDIS_URL=redis://redis:6379/0

# Data & timezone
DATA_DIR=/data
TZ=Asia/Kolkata
CELERY_CONCURRENCY=6

# Gmail endpoints
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
IMAP_HOST=imap.gmail.com
USE_TLS=true

# Auth & security
# APP_SECRET_KEY should be a long random string; it is used for:
# - encrypting stored Gmail app passwords
# - salting user password hashes
# - salting hashed recipient identifiers used for deduplication
APP_SECRET_KEY=change-me-to-a-long-random-string

# Simple built-in user store (username:password pairs, comma-separated).
# On first run, this is written into /data/users.json with salted hashes.
# Example: APP_USERS=admin:SuperSecret123,user1:Password456
APP_USERS=admin:SuperSecret123

# AUTH_MODE:
# - "simple" (default): use username/password defined above or in /data/users.json
# - "none": disable login (NOT recommended for shared deployments)
AUTH_MODE=simple

# Gmail sending guard rails (approximate, for safety)
GMAIL_DAILY_LIMIT=1800
GMAIL_HOURLY_LIMIT=400
GMAIL_WARN_PCT=0.9

# ---------------- Optional quality-of-life ----------------
# (no change needed unless you know you want to tweak)
# ---------------- Google OAuth / Gmail API ----------------
# Use the NEW client you created in Google Cloud for bulk email.

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=
