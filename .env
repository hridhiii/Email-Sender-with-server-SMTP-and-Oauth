# ---------------- Core infra ----------------
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0
REDIS_URL=redis://redis:6379/0
DATA_DIR=/data
TZ=Asia/Kolkata

# CPU parallelism for Celery workers (see sizing table below)
CELERY_CONCURRENCY=6

# ---------------- SMTP/IMAP (Gmail) ----------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
IMAP_HOST=imap.gmail.com
USE_TLS=true

# ---------------- Auth & roles ----------------
# Login mode: "simple" (recommended) or "none"
AUTH_MODE=simple

# First-run ONLY: seeds /data/users.json with hashed passwords.
# Format: username:password pairs, comma-separated.
# Change these to YOUR real users; you can remove this after first boot if you want.
APP_USERS=admin:99261992

# Who can see ALL jobs in Jobs Monitor (comma-separated usernames)
ADMIN_USERS=admin

# Long random string; used for encrypting app passwords + salting hashes.
APP_SECRET_KEY=TXJSjftwPMoOt0zCOd0gnX1HHaLSSQUx

# ---------------- Gmail guard rails (local safety) ----------------
# Pick conservative values; adjust per account/plan.
GMAIL_DAILY_LIMIT=500
GMAIL_HOURLY_LIMIT=150
GMAIL_WARN_PCT=0.85

# ---------------- Optional quality-of-life ----------------
# (no change needed unless you know you want to tweak)
# ---------------- Google OAuth / Gmail API ----------------
# Use the NEW client you created in Google Cloud for bulk email.

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8501/

